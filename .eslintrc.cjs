// Superseded by eslint.config.js (ESLint v9+ flat config). Kept only so
// editors that still look for a legacy config file don't complain; not used
// by the `lint` npm script.
module.exports = {};
